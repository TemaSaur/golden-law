import { Raleway, Prata } from "next/font/google";

export const main = Raleway({ subsets: ["latin", "cyrillic"] });
export const secondary = Prata({ subsets: ["latin", "cyrillic"], weight: "400" });
